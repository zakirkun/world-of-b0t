services:
  internal:
    build:
      context: .
      dockerfile: Dockerfile.internal
    container_name: w0b-w07-internal
    environment:
      W0B_FLAG: ${W0B_FLAG:?must set W0B_FLAG - use manage.ps1/manage.sh}
    networks:
      - w0b
    restart: unless-stopped
    # No ports: this service is reachable ONLY from inside the w0b network.

  w07:
    build: .
    container_name: w0b-w07
    depends_on:
      - internal
    ports:
      - "30007:3000"
    environment:
      W0B_FLAG: ${W0B_FLAG:?must set W0B_FLAG - use manage.ps1/manage.sh}
      INTERNAL_URL: http://w0b-w07-internal:9000
    networks:
      - w0b
    restart: unless-stopped

networks:
  w0b:
    name: w0b
    external: true
