services:
  w19:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: w0b-w19
    environment:
      W0B_FLAG: ${W0B_FLAG:?must set W0B_FLAG - use manage.ps1/manage.sh}
    ports:
      - "30019:80"
    networks:
      - w0b
    restart: unless-stopped

networks:
  w0b:
    name: w0b
    external: true
