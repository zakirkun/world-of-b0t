services:
  internal:
    build:
      context: .
      dockerfile: Dockerfile.internal
    container_name: w0b-w20-internal
    environment:
      W0B_FLAG: ${W0B_FLAG:?must set W0B_FLAG - use manage.ps1/manage.sh}
    networks:
      - w0b
    restart: unless-stopped
    # No ports: this service is reachable ONLY from inside the w0b network.

  w20:
    build: .
    container_name: w0b-w20
    depends_on:
      - internal
    ports:
      - "30020:8080"
    environment:
      W0B_FLAG: ${W0B_FLAG:?must set W0B_FLAG - use manage.ps1/manage.sh}
    networks:
      - w0b
    restart: unless-stopped

networks:
  w0b:
    name: w0b
    external: true
